name: Merge on push to master branch

on:
  push:
    branches:
      - master

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout master branch
        uses: actions/checkout@v2
        with:
          ref: master

      - name: Checkout release branch
        uses: actions/checkout@v2
        with:
          ref: release
          path: release

      - name: Copy files
        run: |
          rsync -av --progress . release --exclude .gitignore --exclude .github
          cd release
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "Merge changes from master branch" || echo "No changes to commit"
          git push origin release